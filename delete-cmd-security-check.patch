From 0cb738466afd666e23538dbe3ecaac47ebbcd3e7 Mon Sep 17 00:00:00 2001
From: shixuantong <shixuantong1@huawei.com>
Date: Wed, 25 Mar 2026 10:01:38 +0800
Subject: [PATCH] delete cmd security check

revert d5c48e8b7181fcfb04eaa2e7797937a2b190b8c1
revert 441c3be68a5296d8da3e24f150b9a6f37702fe2b
---
 src/services/sentryCollector/collect_io.py |  4 ---
 src/services/syssentry/global_values.py    | 10 ++------
 src/services/syssentry/utils.py            | 30 +++-------------------
 3 files changed, 5 insertions(+), 39 deletions(-)

diff --git a/src/services/sentryCollector/collect_io.py b/src/services/sentryCollector/collect_io.py
index 0b3b612..b489794 100644
--- a/src/services/sentryCollector/collect_io.py
+++ b/src/services/sentryCollector/collect_io.py
@@ -20,7 +20,6 @@ import subprocess
 import re
 from typing import Union
 
-from syssentry.utils import is_valid_cmd
 from .collect_config import CollectConfig
 from .collect_config import CONF_IO_NVME_SSD, CONF_IO_SATA_SSD, CONF_IO_SATA_HDD, CONF_IO_THRESHOLD_DEFAULT
 from .collect_plugin import get_disk_type, DiskType
@@ -497,9 +496,6 @@ class CollectIo():
         self
     ) -> None:
         global EBPF_PROCESS
-        if is_valid_cmd(self.ebpf_base_path):
-            logging.error("Invalid ebpf_base_path: %s" % self.ebpf_base_path)
-            return
         try:
             EBPF_PROCESS = subprocess.Popen(
                 self.ebpf_base_path,
diff --git a/src/services/syssentry/global_values.py b/src/services/syssentry/global_values.py
index 0f642ff..21ca5b9 100644
--- a/src/services/syssentry/global_values.py
+++ b/src/services/syssentry/global_values.py
@@ -97,10 +97,7 @@ class InspectTask:
             return 0
         pre_cmd_list = self.task_pre.split(";")
         for pre_cmd_i in pre_cmd_list:
-            try:
-                result = run_cmd(pre_cmd_i)
-            except ValueError:
-                return -1
+            result = run_cmd(pre_cmd_i)
             if result.stderr:
                 logging.error("task %s pre cmd (%s) execute failed, error msg is %s",
                               self.name, pre_cmd_i, result.stderr)
@@ -120,10 +117,7 @@ class InspectTask:
         post_success = True
         post_cmd_list = self.task_post.split(";")
         for post_cmd_i in post_cmd_list:
-            try:
-                result = run_cmd(post_cmd_i)
-            except ValueError:
-                return -1
+            result = run_cmd(post_cmd_i)
             if result.stderr:
                 post_success = False
                 logging.warning("task %s post cmd (%s) execute failed, error msg is %s",
diff --git a/src/services/syssentry/utils.py b/src/services/syssentry/utils.py
index cf10642..6a0defe 100644
--- a/src/services/syssentry/utils.py
+++ b/src/services/syssentry/utils.py
@@ -21,39 +21,21 @@ from datetime import datetime, timezone, timedelta
 MAX_MSG_LEN = 10 * 1024 * 1024  # 10MB
 
 
-def is_valid_cmd(cmd):
-    """check cmd"""
-    if not isinstance(cmd, str):
-        logging.error("cmd must be a string")
-        raise ValueError
-    if not cmd.strip():
-        logging.error("cmd cannot be empty")
-        raise ValueError
-    return True if cmd.isalnum() else False
-
-
 def run_cmd(cmd):
     """run cmd use subprocess.run"""
-    if not is_valid_cmd(cmd):
-        raise ValueError("cmd is illegal")
     result = subprocess.run(shlex.split(cmd), stdout=subprocess.PIPE, stderr=subprocess.PIPE, check=False)
     return result
 
 
 def run_popen(cmd):
     """run cmd use subprocess.Popen"""
-    if not is_valid_cmd(cmd):
-        raise ValueError("cmd is illegal")
     pipe = subprocess.Popen(shlex.split(cmd), stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)
     return pipe
 
 
 def is_exists_cmd(cmd: str) -> bool:
     """Checking Whether a Command Exists in the Environment"""
-    try:
-        res = run_cmd(f"which {cmd}")
-    except ValueError:
-        return False
+    res = run_cmd(f"which {cmd}")
     if res.returncode:
         return False
     return True
@@ -64,10 +46,7 @@ def get_process_pid(process_name):
     process_pid = -1
     if "/" in process_name:
         process_name = process_name.split("/")[-1]
-    try:
-        res = run_cmd('pgrep -x {}'.format(process_name))
-    except ValueError:
-        return -1
+    res = run_cmd('pgrep -x {}'.format(process_name))
     if res.returncode == 0:
         process_pid = res.stdout.decode().strip()
         try:
@@ -86,8 +65,6 @@ def get_current_time_string():
 
 def execute_command(cmd_list):
     try:
-        if not is_valid_cmd("".join(cmd_list)):
-            raise ValueError("cmd is illegal")
         process = subprocess.run(
             cmd_list,
             shell=False,
@@ -100,6 +77,5 @@ def execute_command(cmd_list):
             logging.error("execute command with illegal returncode")
             return None
         return process.stdout
-    except (OSError, ValueError):
+    except OSError:
         logging.error("failed to execute command")
-        return None
-- 
2.27.0

