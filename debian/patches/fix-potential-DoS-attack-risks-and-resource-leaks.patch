From 04d9f95e90ad42f36fdf3a87f5d86c2d0149ca03 Mon Sep 17 00:00:00 2001
From: tong_1001 <shixuantong1@huawei.com>
Date: Wed, 4 Feb 2026 15:32:36 +0800
Subject: [PATCH 06/17] fix potential DoS attack risks and resource leaks

---
 .../python/pySentryCollector/collect_plugin.py      | 11 ++++++++++-
 src/sentryPlugins/cpu_sentry/cpu_alarm.py           |  6 +++++-
 src/services/sentryCollector/collect_server.py      |  6 ++++++
 src/services/syssentry/heartbeat.py                 |  5 +++++
 src/services/syssentry/sentryctl                    | 11 +++++++++++
 src/services/syssentry/syssentry.py                 | 13 ++++++++++---
 src/services/syssentry/utils.py                     |  3 +++
 7 files changed, 50 insertions(+), 5 deletions(-)

diff --git a/src/libsentry/python/pySentryCollector/collect_plugin.py b/src/libsentry/python/pySentryCollector/collect_plugin.py
index 7c2d688..95fa38d 100644
--- a/src/libsentry/python/pySentryCollector/collect_plugin.py
+++ b/src/libsentry/python/pySentryCollector/collect_plugin.py
@@ -18,6 +18,8 @@ import logging
 import re
 import os
 
+from syssentry.utils import MAX_MSG_LEN
+
 COLLECT_SOCKET_PATH = "/var/run/sysSentry/collector.sock"
 
 # data length param
@@ -119,6 +121,7 @@ def client_send_and_recv(request_data, data_str_len, protocol):
 
     res_magic = res_data[:CLT_MSG_MAGIC_LEN]
     if res_magic != "RES":
+        client_socket.close()
         logging.debug("res msg format error")
         return None
 
@@ -126,15 +129,21 @@ def client_send_and_recv(request_data, data_str_len, protocol):
     try:
         protocol_id = int(protocol_str)
     except ValueError:
+        client_socket.close()
         logging.debug("recv msg protocol id is invalid %s", protocol_str)
         return None
 
     if protocol_id >= ClientProtocol.PRO_END:
+        client_socket.close()
         logging.debug("protocol id is invalid")
         return None
 
     try:
         res_data_len = int(res_data[CLT_MSG_MAGIC_LEN+CLT_MSG_PRO_LEN:])
+        if res_data_len < 0 or res_data_len > MAX_MSG_LEN:
+            client_socket.close()
+            logging.error("socket recv data is illegal:%d", res_data_len)
+            return None
         res_msg_data = client_socket.recv(res_data_len)
         res_msg_data = res_msg_data.decode()
         return res_msg_data
@@ -368,4 +377,4 @@ def get_disk_type(disk):
         return result
 
     result['ret'] = ResultMessage.RESULT_SUCCEED
-    return result
\ No newline at end of file
+    return result
diff --git a/src/sentryPlugins/cpu_sentry/cpu_alarm.py b/src/sentryPlugins/cpu_sentry/cpu_alarm.py
index acd8cad..6d76c98 100644
--- a/src/sentryPlugins/cpu_sentry/cpu_alarm.py
+++ b/src/sentryPlugins/cpu_sentry/cpu_alarm.py
@@ -4,7 +4,7 @@ import logging
 import socket
 from enum import Enum
 
-from syssentry.utils import execute_command
+from syssentry.utils import execute_command, MAX_MSG_LEN
 
 MAX_CORE_ID = 1024
 MAX_SOCKET_ID = 255
@@ -236,6 +236,10 @@ def cpu_alarm_recv(server_socket: socket.socket):
         data = client_socket.recv(PARAM_DATA_LEN)
         data_len = check_fixed_param(data, (MIN_DATA_LEN, MAX_DATA_LEN))
 
+        if data_len < 0 or data_len > MAX_MSG_LEN:
+            client_socket.close()
+            logging.error("socket recv data is illegal:%d", data_len)
+            return
         data = client_socket.recv(data_len)
 
         command, event_type, socket_id, core_id = parser_cpu_alarm_info(data)
diff --git a/src/services/sentryCollector/collect_server.py b/src/services/sentryCollector/collect_server.py
index 9a0c886..460fceb 100644
--- a/src/services/sentryCollector/collect_server.py
+++ b/src/services/sentryCollector/collect_server.py
@@ -24,6 +24,8 @@ import select
 import threading
 import time
 
+from syssentry.utils import MAX_MSG_LEN
+
 from .collect_io import IO_GLOBAL_DATA, IO_CONFIG_DATA, IO_DUMP_DATA, DISK_DATA
 from .collect_config import CollectConfig
 
@@ -218,6 +220,10 @@ class CollectServer():
             return
 
         try:
+            if data_len > MAX_MSG_LEN:
+                client_socket.close()
+                logging.error("socket recv data is illegal:%d", data_len)
+                return
             msg_data = client_socket.recv(data_len)
             msg_data_decode = msg_data.decode()
             logging.debug("msg data %s", msg_data_decode)
diff --git a/src/services/syssentry/heartbeat.py b/src/services/syssentry/heartbeat.py
index 0650b8a..dcbbb22 100644
--- a/src/services/syssentry/heartbeat.py
+++ b/src/services/syssentry/heartbeat.py
@@ -21,6 +21,7 @@ import socket
 from .mod_status import set_runtime_status, RUNNING_STATUS, FAILED_STATUS
 from .global_values import SENTRY_RUN_DIR
 from .task_map import TasksMap
+from .utils import MAX_MSG_LEN
 
 THB_SOCKET_PATH = "/var/run/sysSentry/heartbeat.sock"
 THB_MAGIC = 'THB'
@@ -103,6 +104,10 @@ def heartbeat_recv(heartbeat_socket: socket.socket):
         logging.error("heartbeat msg head parse failed")
         client_socket.close()
         return
+    if data_len > MAX_MSG_LEN:
+        client_socket.close()
+        logging.error("socket recv data is illegal:%d", data_len)
+        return
     logging.debug("hb msg data length: %d", data_len)
 
     try:
diff --git a/src/services/syssentry/sentryctl b/src/services/syssentry/sentryctl
index a759d8f..50f9606 100644
--- a/src/services/syssentry/sentryctl
+++ b/src/services/syssentry/sentryctl
@@ -20,6 +20,8 @@ import sys
 import logging
 import json
 
+from syssentry.utils import MAX_MSG_LEN
+
 CTL_SOCKET_PATH = "/var/run/sysSentry/control.sock"
 MAX_PARAM_LENGTH = 256
 
@@ -104,6 +106,10 @@ def client_send_and_recv(request_data, data_str_len):
 
     try:
         client_socket.send(request_msg.encode())
+        if len("CTL") + data_str_len > MAX_MSG_LEN:
+            client_socket.close()
+            logging.error("socket recv data is illegal:%d", len("CTL") + data_str_len)
+            return None
         res_data = client_socket.recv(len("CTL") + data_str_len)
         res_data = res_data.decode()
     except (OSError, UnicodeError):
@@ -114,11 +120,16 @@ def client_send_and_recv(request_data, data_str_len):
     # res: RES{len}{data}
     res_magic = res_data[:3]
     if res_magic != "RES":
+        client_socket.close()
         print("res msg format error")
         return None
 
     try:
         res_data_len = int(res_data[3:])
+        if res_data_len > MAX_MSG_LEN:
+            client_socket.close()
+            logging.error("socket recv data is illegal:%d", res_data_len)
+            return
         res_msg_data = client_socket.recv(res_data_len)
         res_msg_data = res_msg_data.decode()
         return res_msg_data
diff --git a/src/services/syssentry/syssentry.py b/src/services/syssentry/syssentry.py
index ed04a16..6428c73 100644
--- a/src/services/syssentry/syssentry.py
+++ b/src/services/syssentry/syssentry.py
@@ -24,7 +24,6 @@ import fcntl
 import select
 
 from .sentry_config import SentryConfig, get_log_level
-
 from .task_map import TasksMap
 from .global_values import SENTRY_RUN_DIR, SENTRY_RUN_DIR_PERM
 from .cron_process import period_tasks_handle
@@ -43,7 +42,7 @@ from .load_mods import load_tasks, reload_single_mod
 from .heartbeat import (heartbeat_timeout_chk, heartbeat_recv)
 from .result import RESULT_MSG_HEAD_LEN, RESULT_MSG_MAGIC_LEN, RESULT_MAGIC
 from .result import RESULT_LEVEL_ERR_MSG_DICT, ResultLevel
-from .utils import get_current_time_string
+from .utils import get_current_time_string, MAX_MSG_LEN
 from .alarm import alarm_register
 
 from xalarm.register_xalarm import xalarm_unregister
@@ -299,7 +298,10 @@ def server_recv(server_socket: socket.socket):
         client_socket.close()
         logging.error("msg head parse failed")
         return
-
+    if data_len > MAX_MSG_LEN:
+        client_socket.close()
+        logging.error("socket recv data is illegal:%d", data_len)
+        return
     try:
         msg_data = client_socket.recv(data_len)
         msg_data_decode = msg_data.decode()
@@ -317,6 +319,7 @@ def server_recv(server_socket: socket.socket):
         msg_data_decode_dict = json.loads(msg_data_decode)
         cmd_type = msg_data_decode_dict.get("type")
     except json.JSONDecodeError:
+        client_socket.close()
         logging.error("msg data process: msg_data_decode json decode error")
         return
 
@@ -366,6 +369,10 @@ def server_result_recv(server_socket: socket.socket):
         client_socket.close()
         logging.error("msg head parse failed")
         return
+    if data_len > MAX_MSG_LEN:
+        client_socket.close()
+        logging.error("socket recv data is illegal:%d", data_len)
+        return
 
     try:
         msg_data = client_socket.recv(data_len)
diff --git a/src/services/syssentry/utils.py b/src/services/syssentry/utils.py
index 9c27576..cf10642 100644
--- a/src/services/syssentry/utils.py
+++ b/src/services/syssentry/utils.py
@@ -17,6 +17,9 @@ import subprocess
 import shlex
 from datetime import datetime, timezone, timedelta
 
+# Security: Maximum allowed message length to prevent DoS attacks
+MAX_MSG_LEN = 10 * 1024 * 1024  # 10MB
+
 
 def is_valid_cmd(cmd):
     """check cmd"""
-- 
2.27.0

